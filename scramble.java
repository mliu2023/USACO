import java.io.*;
import java.util.*;
public class scramble{
  public static int n;
  public static String minPerm(String s){
    String ans = "";
    ans += Character.toString(s.charAt(i));
    for(int i = 1; i < s.length(); i++){
      char x = s.charAt(i);
      while((int)x<=(int)ans.charAt(0))

    }
  }
  public static void main(String[] args){
    BufferedReader f = new BufferedReader(new FileReader("scramble.in"));
    PrintWriter out = new PrintWriter(new FileWriter("scramble.out"));

  }
}
